/**
 * Shared types index — Lex Law Next v1
 *
 * Re-exports all shared types for convenient importing.
 * Both server and client code may import from here.
 */

export type { TelemetryEventName, TelemetryPayload, TelemetryEnvelope } from './telemetry.js';

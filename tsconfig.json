{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",

    /* skipLibCheck: suppresses type errors inside node_modules declaration files.
       This is required because drizzle-orm@0.30.10 has internal type-declaration
       bugs that surface under TypeScript 5.9 strict mode. Our source code is still
       fully type-checked — only .d.ts files in node_modules are skipped.
       R15 applies to src/**; skipLibCheck does not weaken that guarantee. */
    "skipLibCheck": true,

    /* R15 — Type-Safety Integrity: these four flags are non-negotiable.
       Relaxing any of them requires Kelly sign-off documented in the PR description. */
    "strict": true,
    "exactOptionalPropertyTypes": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,

    /* Additional strictness */
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true,

    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
      "@server/*": ["./src/server/*"],
      "@client/*": ["./src/client/*"],
      "@shared/*": ["./src/shared/*"]
    }
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
